{% extends '::base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('bundles/app/css/young_connection.css') }}">
{% endblock %}

{% block body %}
    {% set ruta = app.request.attributes.get('_route') %}

    <nav class="navbar navbar-default navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                    <span class="sr-only">Desplegar navegación</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="{{ path('inicio') }}">Young Connection</a>
            </div>

            <div id="navbar" class="collapse navbar-collapse">
                <ul class="nav navbar-nav">
                    <li{% if ruta == 'inicio' %} class="active"{% endif %}><a href="{{ path('inicio') }}"><i class="glyphicon glyphicon-home"></i> Inicio </a></li>
                    <li class="{{ 'centro' in ruta ? 'active dropdown' : 'dropdown' }}">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><i class="glyphicon glyphicon-education"></i> Centros <span class="caret"></span></a>
                        <ul class="dropdown-menu" role="menu">
                            <li><a href="{{ path('centros_listar') }}">Listar Centros{% if ruta == 'centros_listar' %} <span class="glyphicon glyphicon-ok"></span>{% endif %}</a></li>
                            <li><a href="{{ path('centro_nuevo') }}">Crear Centro{% if ruta == 'centro_nuevo' %} <span class="glyphicon glyphicon-ok"></span>{% endif %}</a></li>
                        </ul>
                    </li>
                    <li class="{{ 'curso' in ruta ? 'active dropdown' : 'dropdown' }}">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><i class="glyphicon glyphicon-blackboard"></i> Cursos <span class="caret"></span></a>
                        <ul class="dropdown-menu" role="menu">
                            <li><a href="{{ path('cursos_listar') }}">Listar Cursos{% if ruta == 'cursos_listar' %} <span class="glyphicon glyphicon-ok"></span>{% endif %}</a></li>
                            <li><a href="{{ path('curso_nuevo') }}">Crear Curso{% if ruta == 'curso_nuevo' %} <span class="glyphicon glyphicon-ok"></span>{% endif %}</a></li>
                        </ul>
                    </li>
                    <li class="{{ 'alumno' in ruta ? 'active dropdown' : 'dropdown' }}">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><i class="glyphicon glyphicon-user"></i> Alumnos <span class="caret"></span></a>
                        <ul class="dropdown-menu" role="menu">
                            <li><a href="{{ path('alumnos_listar') }}">Listar Alumnos{% if ruta == 'alumnos_listar' %} <span class="glyphicon glyphicon-ok"></span>{% endif %}</a></li>
                            <li><a href="{{ path('alumno_nuevo') }}">Crear Alumno{% if ruta == 'alumno_nuevo' %} <span class="glyphicon glyphicon-ok"></span>{% endif %}</a></li>
                        </ul>
                    </li>
                    <li class="{{ 'mensaje' in ruta ? 'active dropdown' : 'dropdown' }}">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><i class="glyphicon glyphicon-envelope"></i> Mensajes <span class="caret"></span></a>
                        <ul class="dropdown-menu" role="menu">
                            <li><a href="{{ path('mensajes_listar') }}">Listar Mensajes{% if ruta == 'mensajes_listar' %} <span class="glyphicon glyphicon-ok"></span>{% endif %}</a></li>
                            <li><a href="{{ path('mensaje_nuevo') }}">Crear Mensaje{% if ruta == 'mensaje_nuevo' %} <span class="glyphicon glyphicon-ok"></span>{% endif %}</a></li>
                        </ul>
                    </li>
                    <li class="{{ 'usuario' in ruta ? 'active dropdown' : 'dropdown' }}">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><i class="glyphicon glyphicon-user"></i> Usuarios <span class="caret"></span></a>
                        <ul class="dropdown-menu" role="menu">
                            <li><a href="{{ path('usuarios_listar') }}">Listar Usuarios{% if ruta == 'usuarios_listar' %} <span class="glyphicon glyphicon-ok"></span>{% endif %}</a></li>
                            <li><a href="{{ path('usuario_nuevo') }}">Crear Usuario{% if ruta == 'usuario_nuevo' %} <span class="glyphicon glyphicon-ok"></span>{% endif %}</a></li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container">
        {% block content %}{% endblock %}
    </div>

    <footer class="footer">
        <div class="container">
            <p class="text-muted">&copy; 2015, Young Connection</p>
        </div>
    </footer>
{% endblock %}