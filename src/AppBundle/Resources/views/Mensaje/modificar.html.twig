{% extends '::layout.html.twig' %}

{% block title %}Modificar Mensaje {{ mensaje.usuarioDestino }}{% endblock %}

{% block content %}
    <h2 class="well"> Modificar Mensaje {{ mensaje.usuarioDestino }}</h2>

    {{ form(formulario) }}

    <a href="{{ path('mensajes_listar') }}" class="btn btn-info">Volver al listado de Mensajes</a>
{% endblock %}