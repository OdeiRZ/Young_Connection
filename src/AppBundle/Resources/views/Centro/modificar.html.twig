{% extends '::layout.html.twig' %}

{% block title %}Modificar Centro {{ centro.nombre }}{% endblock %}

{% block content %}
    <h1 class="well"> Modificar Centro {{ centro.nombre }}</h1>

    {{ form(formulario) }}

    <a href="{{ path('centros_listar') }}" class="btn btn-info">Volver al listado de Centros</a>
{% endblock %}